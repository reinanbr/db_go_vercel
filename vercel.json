{
  "version": 2,
  "builds": [
    {
      "src": "api/*.go",
      "includeFiles": ["api/db/db.go","api/models/*.go"],
      "use": "@vercel/go"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/main.go"
    }
  ]
}

